# Succinct Data Structures & Complexity Proofs in SCPG

This document details the mathematical algorithms, succinct representations, and complexity proofs underlying the **Succinct Compositional Program Graph (SCPG)** engine.

---

## 1. Succinct AST Encoding via Balanced Parentheses (BP)

### 1.1 Structural Theorem (Munro & Raman, 1997; Sadakane, 2004)

An ordered tree of $n$ nodes can be represented as a sequence of $2n$ balanced parentheses $B \in \{(, )\}^{2n}$ generated by a Depth-First Traversal (DFS):
- Emit `(` on first visit (pre-order entry).
- Emit `)` on backtrack (post-order exit).

```text
Tree Structure:
       Root (0)
      /        \
  ChildA (1)  ChildB (2)
      |
  LeafC (3)

BP Sequence B:
(  (  (  )  )  (  )  )
0  1  3  3  1  2  2  0   <- node pre-order index
```

### 1.2 Auxiliary Rank/Select Operations

By augmenting $B$ with an auxiliary structure of size $O(n / \log n)$ words, the following tree queries execute in **$O(1)$ time**:

| Query Operation | Mathematical Definition | Computational Meaning |
|---|---|---|
| `rank_((B, i)` | $\sum_{j=1}^i [B[j] = '(']$ | Number of open parentheses up to index $i$. |
| `select_((B, k)` | $\min \{ i \mid \text{rank}_((B, i) = k \}$ | Bit position of the $k$-th open parenthesis. |
| `findclose(B, i)` | Matching `)` for `(` at position $i$ | Locates end of subtree rooted at node $i$. |
| `findopen(B, i)` | Matching `(` for `)` at position $i$ | Locates start of subtree. |
| `enclose(B, i)` | Smallest matching pair enclosing $i$ | Computes `parent(v)`. |
| `lca(u, v)` | Minimum excess range query in $B[\text{pre}(u)..\text{pre}(v)]$ | Computes Lowest Common Ancestor. |

### 1.3 Memory Proof

For $n = 1,000,000$ AST nodes:
- Pointer-based AST ({`type`: u32, `parent`: u64, `first_child`: u64, `next_sibling`: u64, `token_id`: u32}):
  $$\text{Size} = 1,000,000 \times 32 \text{ bytes} = \mathbf{32.0 \text{ MB}}$$
- Balanced Parentheses Succinct Representation:
  - Sequence $B$: $2 \times 1,000,000 \text{ bits} = 250 \text{ KB}$
  - Rank/Select auxiliary structure: $\approx 400 \text{ KB}$
  - Parallel pre-order attribute array (`u8` type ID): $1,000,000 \times 1 \text{ byte} = 1.0 \text{ MB}$
  - Monotonic token ranges (`u32` min/max): $2 \times 1,000,000 \times 4 \text{ bytes} = 8.0 \text{ MB}$
  $$\text{Total Size} \approx \mathbf{9.65 \text{ MB}} \quad (\mathbf{3.3\times \text{ to } 128\times \text{ structure compression ratio}})$$

---

## 2. Compressed Sparse Row (CSR) & Wavelet Trees for Multi-Typed Edges

### 2.1 CSR Adjacency Representation

For a graph with $n$ basic blocks and $m$ edges, CSR stores graph structure in two contiguous arrays:
- `offsets[0..n]`: Cumulative out-degree prefix sum (`offsets[v]` is starting index of block $v$'s edges).
- `adj[0..m-1]`: Target vertex IDs for all edges.

Iterating successors of block $v$ scans `adj[offsets[v]..offsets[v+1]-1]` sequentially, maximizing L1/L2 CPU cache hit rates.

### 2.2 Wavelet Tree Edge Filtering

Given edge type alphabet $\Sigma_E$ with $|\Sigma_E| = \sigma = 16$, edge types are stored in a Wavelet Tree over `etype[0..m-1]`:
- Requires $m \lceil \log_2 \sigma \rceil = 4m$ bits.
- `range_count(v_start, v_end, target_type)` computes the exact number of outgoing edges of type `target_type` for basic block $v$ in $O(\log \sigma) = O(4)$ steps.
- Avoids allocating separate adjacency arrays for each edge type ($6\times$ memory reduction).

---

## 3. Deduplicated 64-bit FNV-1a String Interner

### 3.1 Hash Function Formula

$$\text{hash}_0 = \text{0xcbf29ce484222325}$$
$$\text{hash}_{k} = (\text{hash}_{k-1} \oplus \text{byte}_k) \times \text{0x00000100000001B3} \pmod{2^{64}}$$

### 3.2 Open-Addressing Probe & Amortized Complexity

- Open-addressing table with linear probing at load factor limit $\alpha = 0.75$.
- Expected probe length for lookup/insertion:
  $$E[\text{probes}] = \frac{1}{1 - \alpha} = \frac{1}{1 - 0.75} = 4 \text{ probes}$$
- Contiguous storage: `storage: Vec<u8>` holding `[u16_len][utf8_bytes...]` indexed by parallel offset vector `offsets: Vec<u32>`.
- **Lookup Complexity**: $O(|text|)$ amortized time, $O(1)$ random text retrieval by `text_id`.

---

## 4. Reduced Ordered Binary Decision Diagrams (ROBDD)

### 4.1 Shannon Expansion Definition

For a Boolean path feasibility function $f(x_1, x_2, \dots, x_m)$:

$$f = (\bar{x}_1 \land f_{x_1=0}) \lor (x_1 \land f_{x_1=1})$$

ROBDD represents $f$ as a Directed Acyclic Graph (DAG) with:
1. Fixed variable ordering (Reverse Post-Order CFG traversal).
2. Isomorphic sub-graph merging (identical sub-trees collapsed).
3. Redundant node elimination (nodes with identical low and high children eliminated).

### 4.2 Dynamic Sifting Optimization (Rudell, 1993)

To maintain minimal BDD size during construction, Rudell's sifting algorithm dynamically reorders variables:
1. Select variable $x_i$.
2. Swap $x_i$ with adjacent variables to move it to all possible positions in the variable order.
3. Record BDD node count at each position.
4. Move $x_i$ to the position yielding the minimum node count.

Time complexity per variable step: $O(|\text{ROBDD}| \times m)$.

---

## 5. Summary Complexity Bounds

| Sub-System | Time Complexity | Space Complexity |
|---|---|---|
| **Phase 1 Ingestion** | $O(N + n_{\text{tok}} \log n_{\text{tok}})$ | $O(n_{\text{tok}})$ |
| **BP Tree Navigation** | $O(1)$ per operation | $2n_{\text{ast}} + O(n_{\text{ast}}/\log n_{\text{ast}})$ bits |
| **CSR Edge Enumeration** | $O(\text{outdeg}(v))$ | $4n_{\text{bb}} + 4m_{\text{cfg}}$ bytes |
| **Wavelet Edge Filter** | $O(\log \sigma) = O(4)$ | $m \log_2 \sigma$ bits |
| **ROBDD Path Count (#SAT)** | $O(|\text{ROBDD}|)$ | $O(k \cdot 2^w)$ where $w = \text{treewidth}$ |
| **Forward Source Lookup** | $O(\log n_{\text{tok}})$ binary search | $12 n_{\text{tok}}$ bytes |
| **Backward Source Lookup** | $O(1)$ array dereference | $10 n_{\text{tok}}$ bytes |
